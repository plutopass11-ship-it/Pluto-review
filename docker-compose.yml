version: '3.8'

services:
  kitsu-client:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kitsu-client
    restart: always
    network_mode: host
    environment:
      - KITSU_API_URL=http://localhost:3002/api
      - KITSU_EMAIL=plutobase@flyingpluto.ai
      - KITSU_PASSWORD=Styx@001
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
